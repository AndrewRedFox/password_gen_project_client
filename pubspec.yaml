name: password_gen_project
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  grpc: ^3.0.0
  cupertino_icons: ^1.0.2
  protobuf: ^3.1.0
  path_provider: ^2.1.2
  flutter_secure_storage: ^9.0.0
  flutter_spinkit: ^5.2.0

  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:

  uses-material-design: true
  assets:
    - assets/grpc.crt
